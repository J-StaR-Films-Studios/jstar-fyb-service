# 🎯 Task: Deep Research Context Grounding Investigation

**Objective:** Investigate how documents/sources discovered via Deep Research are currently integrated (or failing to integrate) into the Gemini File Search (RAG) context. Improve the pipeline so these discovered sources actually ground the AI generation, rather than just acting as external clickable links.
**Priority:** High
**Scope:** The Deep Research flow (result generation -> saving to library), background extraction (`/api/documents/[id]/extract`), and Gemini File Search Store synchronization.

---

## 📋 Requirements

### Functional Requirements
- **[REQ-001]** Determine the current state of newly saved Deep Research documents (Do they have text extracted? Are they uploaded to the Gemini File Search Store?).
- **[REQ-002]** If Deep Research saves web links (e.g. `sourceType === 'WEB'`) or PDF URLs without downloading the raw PDF buffer, establish a mechanism to fetch that data or gracefully fallback so the AI can actually read the content.
- **[REQ-003]** Implement a background sync or extraction pipeline specifically for Deep Research results so that clicking "Save to Project" guarantees the content enters the AI's RAG context.
- **[REQ-004]** Add UI indicators in the Research Library (or Research Panel) that explicitly show if a Deep Research source has been successfully vectorized and grounded for the AI.

### Technical Requirements
- **[TECH-001]** Review the API that saves deep research results (e.g., `src/app/api/projects/[id]/research/...`).
- **[TECH-002]** Review Gemini File Search integration in `src/lib/gemini-file-search.ts` and ensure it supports or rejects the shapes of data provided by Deep Research.
- **[TECH-003]** Handle fetch/CORS/paywall limits cleanly if we scrape remote PDFs returned by Semantic Scholar/Google.

---

## 🏗️ Implementation Plan

### Phase 1: Investigation & Discovery
- [ ] Analyze the API endpoint used to save Deep Research results to the database.
- [ ] Check if `fileData` or `extractedContent` is populated, or if only a `fileUrl` / `snippet` is saved.
- [ ] Verify if the `importedToFileSearch` flag ever turns `true` for these documents.

### Phase 2: Pipeline Design
- [ ] Design a server-side fetching mechanism to grab the actual text of Deep Research results (e.g., fetching the raw PDF via backend if open access).
- [ ] Decide when this fetch happens (synchronously during save, or via a background extraction route).

### Phase 3: Core Implementation
- [ ] Update the save API to trigger text extraction and Gemini Store upload for selected deep research papers.
- [ ] Fallback: If a PDF cannot be downloaded (paywall or cors block), extract the *abstract/snippet* heavily and feed *that* to the Gemini File Search as a lightweight text document to ensure *some* context is grounded.

### Phase 4: UI & Testing
- [ ] Update `ResearchDocumentItem.tsx` status indicators for these types of files.
- [ ] Test the full generation flow: Run deep research -> save paper -> ask the Builder AI specifically about an obscure detail from that paper to prove grounding works.

---

## 📁 Files to Create/Modify

| File | Action | Purpose |
|------|--------|---------|
| `src/app/api/projects/[id]/research/sync/route.ts` (or similar save API) | Modify | Fix integration logic, trigger AI extraction |
| `src/app/api/documents/[id]/extract/route.ts` | Review/Modify | Need support for external links |
| `src/features/builder/components/ResearchDocumentItem.tsx` | Modify | Update Status UI |

---

## ✅ Success Criteria

### Code Quality
- [ ] TypeScript compliant (no `any`)
- [ ] Solid error boundaries around external fetch attempts (don't crash the UI if a PDF is 404/paywalled).

### Functionality
- [ ] Deep research sources demonstrably impact the Gemini AI generation in the Builder.
- [ ] Clear visual feedback on whether a source is "AI Ready" vs just a "Reference Link".

---

## 🔗 Dependencies

**Depends on:**
- Existing extraction logic (`extractPdfText`, `mammoth`)

**Related files:**
- `src/lib/gemini-file-search.ts`

---

## 🚀 Getting Started

1. Read this task prompt completely.
2. Investigate the Deep Research saving architecture first (`api/projects/[id]/research`).
3. Provide progress updates after discovery.
4. Deliver final results with documentation.

---

*Generated by /vibe-spawnTask workflow*
