# Task: Integration & Polish (State-Based Rendering, Animations, Responsive)

**Session ID:** org-20250219-builder-ui-rebuild
**Source:** Orchestrator
**Context:** Complete Builder UI Transformation
**Priority:** P1
**Dependencies:** All previous tasks (1-8)
**Created At:** 2026-02-19T09:24:00+01:00

---

## Objective

Integrate all components into a cohesive builder experience with proper state-based rendering, animations, and responsive behavior. This is the "glue" task that ensures everything works together.

## Scope

**In Scope:**
1. State-Based Rendering in `ChapterOutliner.tsx`:
   - **Locked State** (not paid): Outline Preview + Paywall Card
   - **Unlocked State** (paid, DIY mode): Outline Preview + Chapter Generator + Secondary Actions + UpsellBridge
   - **Mode Selection** (paid, no mode): ModeSelection component
   - **Concierge Mode** (paid, CONCIERGE): ConciergeWaiting component

2. Secondary Actions Grid Layout:
   - 2-column grid on desktop (Research Sources + Quick Actions)
   - Single column on mobile
   - Proper spacing and alignment

3. Content Reordering (DIY mode):
   - Outline Preview (top)
   - Chapter Generator (primary position)
   - Secondary Actions Grid (Research + Quick Actions)
   - UpsellBridge (bottom - preserved)

4. Animations:
   - Fade in on page load (staggered)
   - Panel slide from right
   - Collapsible sections (max-height transition)
   - Chapter progress bar fill
   - Spinner rotation for "Generating" state

5. Responsive Behavior:
   - Desktop (1280px+): Full header, 2-column grid, visible chat FAB
   - Tablet (768-1279px): Compact header, 2-column grid
   - Mobile (<768px): Compact header, single column, bottom nav

6. Safe Bottom Padding:
   - Content has `pb-20` or `pb-safe` for mobile nav
   - No content hidden behind nav

**Out of Scope:**
- New component creation
- API changes
- State management changes

## Context

### Current State
- All components created in previous tasks
- Need to be integrated properly
- State-based rendering logic exists but needs refinement

### Mockup Reference
- Full page layout: Lines 200-723 of `mockup-refined-builder.html`
- Design spec: Lines 56-95 of `DESIGN-SPEC-BUILDER.md`

### Animation Guidelines
```css
/* Transitions */
transition: all 0.2s ease;      /* Hover states */
transition: all 0.3s ease;      /* Panel slides */
transition: all 0.4s ease;      /* Section collapse */

/* Key animations */
1. Fade in on page load (staggered, 0.1s delay between elements)
2. Panel slide from right (transform: translateX)
3. Collapsible sections (max-height transition)
4. Chapter progress bar fill animation
5. Spinner rotation for "Generating" state
```

## Definition of Done

- [ ] State-based rendering works correctly (locked/unlocked/mode selection)
- [ ] Secondary actions grid: 2 columns desktop, 1 column mobile
- [ ] Content order matches mockup (Outline â Chapters â Actions â Upsell)
- [ ] UpsellBridge preserved at bottom
- [ ] Fade-in animations on page load
- [ ] Panel slide animation smooth
- [ ] Collapsible sections animate
- [ ] Progress bar animates
- [ ] Responsive breakpoints work correctly
- [ ] Safe bottom padding for mobile nav
- [ ] No content hidden behind nav
- [ ] `npx tsc --noEmit` passes

## Expected Artifacts

| File | Action | Purpose |
|------|--------|---------|
| `src/features/builder/components/ChapterOutliner.tsx` | MODIFY | State-based rendering, content order |
| `src/app/(saas)/project/builder/BuilderClient.tsx` | MODIFY | Integration, animations |

## Constraints

- ONLY perform the work outlined above
- Do NOT create new components
- Do NOT change existing logic
- Signal completion using `attempt_completion` tool
- Create `09_integration_polish.result.md` file with summary when complete

---
*Generated by vibe-orchestrator mode*