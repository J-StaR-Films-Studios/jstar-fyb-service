# Task: Circular Progress Stepper

**Session ID:** org-20250219-builder-ui-rebuild
**Source:** Orchestrator
**Context:** Complete Builder UI Transformation
**Priority:** P0
**Dependencies:** Task 1 (Foundation & Layout)
**Created At:** 2026-02-19T09:17:00+01:00

---

## Objective

Implement the circular progress stepper that shows the 3-step workflow (Concept/Strategy/Blueprint) in the builder header. This replaces the current inline stepper in BuilderClient.

## Scope

**In Scope:**
1. Create `ProgressStepper.tsx` component with:
   - 3 circular steps: Concept, Strategy, Blueprint
   - Connecting lines between steps (gradient when active)
   - Completed state: green circle with checkmark
   - Active state: purple circle with pulse animation
   - Pending state: gray circle with number

2. Desktop layout:
   - Horizontal layout with connecting lines
   - Labels below each step
   - Full visibility of all 3 steps

3. Mobile layout:
   - Compact horizontal layout
   - Step dots only (no labels on mobile)
   - Or: Progress bar alternative

4. Integrate into `BuilderHeader.tsx`:
   - Center position on desktop
   - Hidden or compact on mobile

**Out of Scope:**
- Header layout changes (Task 1)
- Content changes in BuilderClient
- Step navigation logic (already exists in store)

## Context

### Current State
- `BuilderClient.tsx` has inline stepper (lines 157-198)
- STEPS array exists: `[{ id: 'TOPIC', label: 'Concept' }, { id: 'ABSTRACT', label: 'Strategy' }, { id: 'OUTLINE', label: 'Blueprint' }]`
- `useBuilderStore` has `step` state: 'TOPIC' | 'ABSTRACT' | 'OUTLINE'

### Mockup Reference
- Progress stepper: Lines 389-429 of `mockup-refined-builder.html`
- Design spec: Lines 148-158 of `DESIGN-SPEC-BUILDER.md`

### Key Styles from Mockup
```html
<!-- Connecting Line Background -->
<div class="absolute top-4 left-10 right-10 h-0.5 bg-white/10 -z-10"></div>
<!-- Connecting Line Active (gradient) -->
<div class="absolute top-4 left-10 right-10 h-0.5 bg-gradient-to-r from-green-500 via-green-500 to-primary -z-10"></div>

<!-- Step: Completed -->
<div class="w-8 h-8 rounded-full bg-green-500 flex items-center justify-center text-black shadow-[0_0_15px_rgba(34,197,94,0.4)]">
    <i data-lucide="check" class="w-4 h-4 stroke-[3]"></i>
</div>
<span class="text-[10px] font-bold uppercase tracking-wider text-green-400">Concept</span>

<!-- Step: Active -->
<div class="w-8 h-8 rounded-full bg-primary text-white flex items-center justify-center shadow-[0_0_20px_rgba(139,92,246,0.5)] relative">
    <div class="absolute inset-0 bg-primary/20 animate-ping rounded-full"></div>
</div>

<!-- Step: Pending -->
<div class="w-8 h-8 rounded-full border-2 border-white/10 text-gray-500">3</div>
```

## Definition of Done

- [ ] `ProgressStepper.tsx` created with 3 states (completed/active/pending)
- [ ] Desktop: Horizontal layout with labels, connecting lines
- [ ] Mobile: Compact layout (dots only or progress bar)
- [ ] Integrated into `BuilderHeader.tsx`
- [ ] Inline stepper removed from `BuilderClient.tsx`
- [ ] Animations: pulse on active, smooth transitions
- [ ] `npx tsc --noEmit` passes

## Expected Artifacts

| File | Action | Purpose |
|------|--------|---------|
| `src/features/builder/components/ProgressStepper.tsx` | CREATE | Circular progress stepper |
| `src/features/builder/components/BuilderHeader.tsx` | MODIFY | Integrate ProgressStepper |
| `src/app/(saas)/project/builder/BuilderClient.tsx` | MODIFY | Remove inline stepper |

## Constraints

- ONLY perform the work outlined above
- Use existing `useBuilderStore.step` for state
- Match mockup styling exactly
- Signal completion using `attempt_completion` tool
- Create `02_progress_stepper.result.md` file with summary when complete

---
*Generated by vibe-orchestrator mode*