# Task: Foundation & Layout Wrapper

**Session ID:** org-20250219-builder-ui-rebuild
**Source:** Orchestrator
**Context:** Complete Builder UI Transformation
**Priority:** P0
**Dependencies:** None
**Created At:** 2026-02-19T09:15:00+01:00

---

## Objective

Create the foundation for the builder page transformation by setting up the layout wrapper, integrating with the existing SaasShell, and establishing the builder-specific context that all other components will use.

## Scope

**In Scope:**
1. Create `BuilderLayoutContext.tsx` - A context provider that:
   - Tracks research panel open/close state
   - Provides project data to all builder components
   - Manages the "locked" vs "unlocked" state visibility

2. Modify `src/app/(saas)/project/layout.tsx` to:
   - Pass `hideBottomNav={true}` to `SaasShell` when on builder route
   - Pass `headerContent={<BuilderHeader />}` to `SaasShell`
   - Wrap children in `BuilderLayoutContext`

3. Create `BuilderHeader.tsx` - The builder-specific header that:
   - Shows logo + project title + save status (left side)
   - Has a placeholder for progress stepper (Task 2 will implement)
   - Has research button with badge count (right side)
   - Works on both desktop and mobile

4. Add ambient background blobs to the builder page

**Out of Scope:**
- Progress stepper implementation (Task 2)
- Floating research panel (Task 4)
- Mobile bottom nav (Task 8)
- Any content changes to ChapterOutliner

## Context

### Current State
- `SaasShell.tsx` accepts `headerContent?: ReactNode` prop (line 43)
- `SaasShell.tsx` accepts `hideBottomNav?: boolean` prop (line 47)
- `BuilderClient.tsx` has inline progress stepper that should move to header
- No context exists for sharing builder state across components

### Mockup Reference
- Ambient background: Lines 202-204 of `mockup-refined-builder.html`
- Header structure: Lines 340-384 of `mockup-refined-builder.html`

### Key Files
- `src/features/ui/SaasShell.tsx` - Global shell (already has props we need)
- `src/app/(saas)/project/layout.tsx` - Project layout (modify to pass props)
- `src/app/(saas)/project/builder/BuilderClient.tsx` - Builder client (will consume context)
- `src/features/builder/components/BuilderHeader.tsx` - [NEW] Builder header
- `src/features/builder/context/BuilderLayoutContext.tsx` - [NEW] Context provider

## Definition of Done

- [ ] `BuilderLayoutContext.tsx` created with:
  - `isResearchPanelOpen` state + toggle function
  - `projectData` from useBuilderStore
  - `isPaid` state from useBuilderStore
- [ ] `BuilderHeader.tsx` created with:
  - Logo + project title (truncated on mobile)
  - Save status indicator (green dot + "Saved")
  - Research button with badge count (from store)
  - Menu button (mobile)
- [ ] `project/layout.tsx` modified to:
  - Conditionally hide global `MobileBottomNav` on builder route
  - Pass `BuilderHeader` as `headerContent` to `SaasShell`
- [ ] Ambient background blobs added to builder page
- [ ] `npx tsc --noEmit` passes

## Expected Artifacts

| File | Action | Purpose |
|------|--------|---------|
| `src/features/builder/context/BuilderLayoutContext.tsx` | CREATE | Shared builder state context |
| `src/features/builder/components/BuilderHeader.tsx` | CREATE | Builder-specific header |
| `src/app/(saas)/project/layout.tsx` | MODIFY | Pass props to SaasShell |
| `src/app/(saas)/project/builder/BuilderClient.tsx` | MODIFY | Consume context, remove inline stepper |

## Constraints

- ONLY perform the work outlined above
- Do NOT modify any content rendering logic in ChapterOutliner
- Do NOT break the existing SaasShell behavior on other routes
- Signal completion using `attempt_completion` tool
- Create `01_foundation_layout.result.md` file with summary when complete

---
*Generated by vibe-orchestrator mode*