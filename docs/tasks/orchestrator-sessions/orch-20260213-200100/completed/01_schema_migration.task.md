# Task: Schema Migration — ResearchDocument Enhancements

**Session ID:** orch-20260213-200100
**Source:** Orchestrator (Hybrid Research Feature - Option 3)
**Context:** The resource search feature is being rebuilt using Semantic Scholar API + Gemini Grounding. The existing `ResearchDocument` model needs new fields to support structured academic metadata and source categorization.
**Priority:** P0 (Foundation — blocks all other implementation tasks)
**Dependencies:** None
**Created At:** 2026-02-13T20:01:00

---

## 📋 Objective

Add new fields to the `ResearchDocument` Prisma model to support the Hybrid Research feature. These fields store academic metadata from Semantic Scholar and categorize sources by type.

## 🎯 Scope

**In Scope:**
- Modify `prisma/schema.prisma` → `ResearchDocument` model
- Run `pnpm prisma migrate dev --name add_research_metadata`
- Run `pnpm prisma generate`
- Verify no TypeScript errors with `npx tsc --noEmit`

**Out of Scope:**
- Modifying any service files
- Modifying any UI components
- Changing any API routes

## 📚 Context

### Current ResearchDocument Schema (line 197 in schema.prisma)
```prisma
model ResearchDocument {
  id                   String    @id @default(cuid())
  projectId            String
  fileUrl              String?
  fileData             Bytes?
  mimeType             String?
  fileName             String
  fileType             String
  status               String    @default("PENDING")
  processedAt          DateTime?
  title                String?
  author               String?
  year                 String?
  // ... (many more existing fields)
  importedToFileSearch Boolean   @default(false)
  fileSearchFileId     String?
  importError          String?
  createdAt            DateTime  @default(now())
  updatedAt            DateTime  @updatedAt
  project              Project   @relation(fields: [projectId], references: [id], onDelete: Cascade)
  @@index([projectId])
}
```

### New Fields to Add

| Field | Type | Description |
|-------|------|-------------|
| `sourceType` | `String @default("WEB")` | `"ACADEMIC"` or `"WEB"` |
| `citationCount` | `Int?` | Citation count from Semantic Scholar |
| `openAccessUrl` | `String?` | Direct open-access PDF URL |
| `abstractText` | `String?` | Paper abstract |
| `snippet` | `String?` | AI-generated relevance summary |
| `authors` | `String?` | Comma-separated author names |
| `venue` | `String?` | Journal/conference name |
| `semanticScholarId` | `String?` | Paper ID for deduplication |

---

## ✅ Definition of Done

- [x] New fields added to `ResearchDocument` model
- [x] Migration created and applied: `pnpm prisma db push` (used db push for non-interactive env)
- [x] Prisma client regenerated: `pnpm prisma generate`
- [x] TypeScript passes: `npx tsc --noEmit`
- [x] No existing fields removed or renamed

## 📁 Expected Artifacts

| File | Purpose |
|------|---------|
| `prisma/schema.prisma` | Updated model |
| `prisma/migrations/XXXXX_add_research_metadata/migration.sql` | Migration |
| `docs/tasks/orchestrator-sessions/orch-20260213-200100/completed/01_schema_migration.result.md` | Results |

## 🚫 Constraints

- Do NOT remove or rename any existing fields
- Do NOT modify any other models
- All new fields must be optional or have defaults (backward compatible)
- Create `.result.md` file with verification output

---
*Generated by vibe-orchestrator mode*
